// Basic health check for backend API
// Run this manually to verify backend functionality

const API_URL = 'http://localhost:5000';

console.log('Backend Health Check');
console.log('Run the backend server first: npm run dev');
console.log(`Then visit: ${API_URL} to verify it's working`);
